{
  "name": "cc-monitor",
  "displayName": "CC Monitor",
  "description": "Monitor Claude Code usage and costs in VSCode status bar (thinkcyte internal fork of GrowthJack/ClaudeCodeUsage)",
  "version": "2.1.0",
  "publisher": "thinkcyte",
  "author": "thinkcyte",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yuzuponikemi/cc-monitor.git"
  },
  "bugs": {
    "url": "https://github.com/yuzuponikemi/cc-monitor/issues"
  },
  "homepage": "https://github.com/yuzuponikemi/cc-monitor",
  "keywords": [
    "claude",
    "claude-code",
    "usage",
    "cost",
    "monitoring",
    "ai",
    "anthropic",
    "token",
    "status-bar"
  ],
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other",
    "Visualization"
  ],
  "icon": "icon.png",
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Claude Code Usage",
      "properties": {
        "claudeCodeUsage.refreshInterval": {
          "type": "number",
          "default": 60,
          "minimum": 30,
          "description": "Refresh interval in seconds (minimum 30 seconds)"
        },
        "claudeCodeUsage.dataDirectory": {
          "type": "string",
          "default": "",
          "description": "Custom Claude data directory path (leave empty for auto-detection)"
        },
        "claudeCodeUsage.language": {
          "type": "string",
          "enum": [
            "auto",
            "en",
            "zh-TW",
            "zh-CN",
            "ja",
            "ko"
          ],
          "default": "auto",
          "description": "Display language (auto-detection or manual selection)"
        },
        "claudeCodeUsage.decimalPlaces": {
          "type": "number",
          "default": 2,
          "minimum": 0,
          "maximum": 4,
          "description": "Number of decimal places for cost display"
        },
        "claudeCodeUsage.compactNumbers": {
          "type": "boolean",
          "default": false,
          "description": "Show large token counts compactly (e.g. 1.2M, 345K) instead of full numbers."
        },
        "claudeCodeUsage.timezone": {
          "type": "string",
          "default": "",
          "description": "IANA timezone for date display (e.g. \"Asia/Hong_Kong\", \"UTC\"). Leave empty to use the system timezone. Useful inside sandboxes/devcontainers. Day-boundary calculations still follow the system timezone."
        },
        "claudeCodeUsage.usageLimitTracking": {
          "type": "boolean",
          "default": true,
          "description": "Show real 5-hour and weekly limit utilisation in the status bar. Uses Claude Code's existing sign-in (~/.claude/.credentials.json) — no configuration needed."
        },
        "claudeCodeUsage.recordQuotaHistory": {
          "type": "boolean",
          "default": true,
          "description": "Record a timestamped history of real 5-hour/weekly quota utilisation to ~/.claude/cc-monitor/quota-history.jsonl for the Quota tab and export. History only accrues while the extension is running and cannot be back-filled. Requires usageLimitTracking."
        },
        "claudeCodeUsage.enableContentAnalysis": {
          "type": "boolean",
          "default": true,
          "description": "Run the content/token analysis on conversation logs and show the Content tab. Disable to skip the CPU-heavy text scan during refresh."
        },
        "claudeCodeUsage.enableContextHealth": {
          "type": "boolean",
          "default": true,
          "description": "Show the live Context Health indicator in the status bar. It estimates — offline, from the active session's log — how full the context window is, what is filling it, and whether it shows signs of 'context rot' (a good cue to /clear). Disable to hide it and skip its per-session analysis during refresh."
        },
        "claudeCodeUsage.contextHealthRotNotification": {
          "type": "boolean",
          "default": false,
          "description": "Pop a one-time notification when a session first shows 'context rot'. Debounced to at most once per session (re-armed after a 30-minute quiet gap). Requires enableContextHealth."
        },
        "claudeCodeUsage.sessionCardRecencyMinutes": {
          "type": "number",
          "default": 60,
          "minimum": 1,
          "description": "Show a separate status-bar card for each Claude Code session whose most recent request is within this many minutes. Older sessions drop off the bar. Each card shows that session's own model, context fill, and prompt-cache warmth."
        },
        "claudeCodeUsage.maxSessionCards": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "description": "Maximum number of per-session cards shown in the status bar at once (most recently active first), so a machine running many parallel sessions does not overflow the bar."
        },
        "claudeCodeUsage.quotaThresholdNotification": {
          "type": "boolean",
          "default": true,
          "description": "Pop a warning when the 5-hour quota crosses 80% (and again at 95%), so heavy tasks can be paused or deferred before the window is exhausted. Re-armed when the quota resets. Requires usageLimitTracking."
        },
        "claudeCodeUsage.exportInsights": {
          "type": "boolean",
          "default": true,
          "description": "Write a machine-readable snapshot of the computed usage analysis to ~/.claude/cc-monitor/insights/latest.json on each refresh, for use by the cc-monitor Claude Code skills (/cc-usage-advice etc.). Local file only — nothing is transmitted."
        },
        "claudeCodeUsage.projectGroupingMode": {
          "type": "string",
          "enum": [
            "git",
            "folder",
            "flat"
          ],
          "default": "git",
          "description": "How the Projects tab groups working directories. 'git' groups by enclosing git repository (default); 'folder' groups by the heuristic top-level project folder only (no filesystem walk); 'flat' shows every working directory as its own row."
        }
      }
    },
    "commands": [
      {
        "command": "claudeCodeUsage.refresh",
        "title": "Refresh Usage Data",
        "category": "Claude Code Usage"
      },
      {
        "command": "claudeCodeUsage.showDetails",
        "title": "Show Usage Details",
        "category": "Claude Code Usage"
      },
      {
        "command": "claudeCodeUsage.openSettings",
        "title": "Open Settings",
        "category": "Claude Code Usage"
      },
      {
        "command": "claudeCodeUsage.showLogs",
        "title": "Show Diagnostic Logs",
        "category": "Claude Code Usage"
      },
      {
        "command": "claudeCodeUsage.exportQuotaHistory",
        "title": "Export Quota History",
        "category": "Claude Code Usage"
      },
      {
        "command": "claudeCodeUsage.openQuotaHistoryFile",
        "title": "Open Quota History File",
        "category": "Claude Code Usage"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "npx --yes @vscode/vsce package --allow-missing-repository -o cc-monitor.vsix",
    "reinstall": "npm run package && code --install-extension cc-monitor.vsix --force"
  },
  "devDependencies": {
    "@types/glob": "^9.0.0",
    "@types/node": "24.0.15",
    "@types/vscode": "^1.74.0",
    "typescript": "^5.8.3"
  }
}